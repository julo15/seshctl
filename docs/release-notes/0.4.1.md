# Seshctl 0.4.1

Smoke-test release for the Sparkle auto-update flow.

No functional changes vs 0.4.0 — this release exists to verify that an installed 0.4.0 build detects the new appcast entry, downloads the signed DMG, verifies the EdDSA signature, swaps `/Applications/Seshctl.app`, and relaunches cleanly at 0.4.1.

If you're seeing this prompt: the loop works end-to-end.
